# Example Environment Configuration for Email Verification
# Copy this to .env and fill in your actual values

# Server Configuration
PORT=5000
ORIGIN=0.0.0.0

# MongoDB
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/
DB_NAME=facialderma_db

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_DAYS=7

# Email Configuration (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SKIP_EMAIL=False

# Email Verification
FRONTEND_URL=http://localhost:3000
VERIFICATION_TOKEN_EXPIRY_MINUTES=15

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ML Model Configuration
PYTORCH_MODEL_PATH=best_model.pth
BLUR_THRESHOLD=50.0
MIN_FACE_AREA_RATIO=0.25

# ============================================
# SETUP INSTRUCTIONS
# ============================================

# 1. Gmail App Password Setup:
#    - Go to Google Account settings
#    - Security > 2-Step Verification (must be enabled)
#    - Security > App passwords
#    - Generate new app password for "Mail"
#    - Copy the 16-character password (remove spaces)
#    - Paste into EMAIL_PASS above

# 2. Frontend URL:
#    - Development: http://localhost:3000
#    - Production: https://yourdomain.com
#    - Must match your React app's URL

# 3. Token Expiry:
#    - Recommended: 15-60 minutes
#    - Shorter = more secure but may frustrate users
#    - Longer = more convenient but less secure

# 4. MongoDB:
#    - Use MongoDB Atlas for production
#    - Ensure network access is configured
#    - Create database user with read/write permissions
